import { Typography } from '@mui/material';
import React from 'react';

const NotFoundPage = () => {
    return (
        <div>
            <Typography variant="h3" style={{color:"red", textAlign:"center"}}>404 Page Not Found</Typography>
        </div>
    );
};

export default NotFoundPage;